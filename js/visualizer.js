//========== main ==================

var main = $( '<div>');
  main
    .addClass( 'main' );
  $( 'body' ).append( main );

//========== viz ===================

var visualizeIt = $( '<div>' );
  visualizeIt
    .addClass ( 'visualizeIt' );
  $( main ).append( visualizeIt );

//========== sorter button =========
